{
  "name": "Python 3",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "Manyue_ML_Portfolio.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter",
        "ms-azuretools.vscode-docker",
        "ms-python.black-formatter",
        "ms-python.isort",
        "ms-python.flake8"
      ]
    }
  },
  "updateContentCommand": "[ -f packages.txt ] && sudo apt update && sudo apt upgrade -y && sudo xargs apt install -y <packages.txt; [ -f requirements.txt ] && pip3 install --user -r requirements.txt; pip3 install --user streamlit; echo '✅ Packages installed and Requirements met'",
  "postAttachCommand": {
    "server": "streamlit run Manyue_ML_Portfolio.py --server.enableCORS false --server.enableXsrfProtection false"
  },
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ],
  "tasks": {
    "test": "#!/bin/bash echo \"Running tests...\" # Example: You could use pytest or other testing frameworks here # streamlit run tests/test_portfolio.py  (If you have tests) echo \"Tests complete.\"",
    "build": "#!/bin/bash echo \"Building your portfolio...\" # Add any build commands here, e.g., compiling assets, etc. echo \"Build complete.\"",
    "launch": "#!/bin/bash echo \"Starting Streamlit app...\" source .venv/bin/activate # Activate virtual environment streamlit run Manyue_ML_Portfolio.py --server.enableCORS false --server.enableXsrfProtection false"
  }
}
